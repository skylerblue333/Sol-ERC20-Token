{
  "name": "skycoin4444-erc20-lab",
  "private": true,
  "version": "0.2.0",
  "description": "Truthful ERC-20 engineering lab for SKYCOIN4444 integration experiments",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "verify": "npm run compile && npm test && npm audit --audit-level=high"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^2.0.8",
    "@nomicfoundation/hardhat-ethers": "^3.0.8",
    "@openzeppelin/contracts": "^5.4.0",
    "chai": "^4.5.0",
    "ethers": "^6.13.4",
    "hardhat": "^2.22.17"
  }
}
